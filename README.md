
# Purpose
Dockerfile extensions for ddev when running a Symfony project fully inside ddev. 

Installs the following if missing:

- symfony-cli
- amqp PHP extension

# How to
Copy the ./ddev/web-build/Dockerfile file (or its content) to your ddev project at the same place and start ddev again.
